#include "main.h"

/**
 * _strcpy - function
 * @dest: parameter
 * @src: parameter
 * Return: Value
 */

char *_strcpy(char *dest, char *src)
{
	int x = 0;

	while (i >= 0)
	{
		*(dest + x) = *(src + i);
		if (*(src + i) == '\0')
		{
			break;
		}

	return (dest);
}
